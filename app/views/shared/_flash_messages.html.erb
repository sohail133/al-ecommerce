<% if notice %>
  <script>
    (function() {
      const container = document.getElementById('flash-toast-container');
      if (container) {
        const toast = document.createElement('div');
        toast.innerHTML = '<%= j render("shared/flash_toast_message", type: :notice, message: notice, id: "flash-toast-#{SecureRandom.hex(4)}") %>';
        const toastElement = toast.firstElementChild;
        if (toastElement) {
          container.appendChild(toastElement);
        }
      }
    })();
  </script>
<% end %>

<% if alert %>
  <script>
    (function() {
      const container = document.getElementById('flash-toast-container');
      if (container) {
        const toast = document.createElement('div');
        toast.innerHTML = '<%= j render("shared/flash_toast_message", type: :alert, message: alert, id: "flash-toast-#{SecureRandom.hex(4)}") %>';
        const toastElement = toast.firstElementChild;
        if (toastElement) {
          container.appendChild(toastElement);
        }
      }
    })();
  </script>
<% end %>
