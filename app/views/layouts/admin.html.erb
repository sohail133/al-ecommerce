<!DOCTYPE html>
<html lang="en" data-controller="admin">
  <head>
    <title><%= content_for(:title) || "Admin Dashboard - Al Ecommerce" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    <style>
      button, a, input, select, textarea, [role="button"], [tabindex] {
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      }
    </style>
  </head>

  <body class="bg-pink-lightest dark:bg-pink-lightest dark:text-black min-h-screen">
    <div class="flex h-screen overflow-hidden">
      <%= render "admin/shared/sidebar" %>

      <div class="flex flex-col flex-1 overflow-hidden">
        <%= render "admin/shared/header" %>

        <main class="flex-1 overflow-y-auto bg-pink-lightest dark:bg-pink-lightest px-4 lg:px-6 py-6">
          <div class="max-w-full">
            <%= yield %>
          </div>
        </main>
      </div>
    </div>

    <div data-admin-target="sidebarOverlay" data-action="click->admin#closeSidebar" class="fixed inset-0 bg-black bg-opacity-50 z-40 lg:hidden hidden"></div>
  </body>
</html>

